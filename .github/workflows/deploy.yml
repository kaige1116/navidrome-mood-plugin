name: Build and Deploy Plugin

on:
  push:
    branches: [ "master" ]
  workflow_dispatch:

jobs:
  call-shared-workflow:
    permissions:
      contents: read
      packages: write
      
    uses: RFLundgren/automation/.github/workflows/reusable-docker-push.yml@main
    with:
      image_name: "navidrome-mood-plugin"
      # ADD THIS LINE:
      build_context: "./analyzer-service"
    secrets: inherit
